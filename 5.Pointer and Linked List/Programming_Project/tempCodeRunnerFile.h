    // if(isValidLoc( r-1, c)) {
                //     Location2D p(r-1,c);
                //     locStack.push(p.getLink());
                // }
                // if(isValidLoc( r+1, c)) {
                //     Location2D p(r+1, c);
                //     locStack.push(p.getLink());
                // }
                // if(isValidLoc( r, c-1)) {
                //     Location2D p(r, c-1);
                //     locStack.push(p.getLink());
                // }
                // if(isValidLoc( r, c+1)) {
                //      Location2D p(r, c+1);
                //      locStack.push(p.getLink()); 
                //     }